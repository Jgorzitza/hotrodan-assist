import React from 'react';
{{#if withStyles}}import styles from './{{pascalCase name}}.module.css';{{/if}}

export interface {{pascalCase name}}Props {
  children?: React.ReactNode;
  className?: string;
}

export const {{pascalCase name}}: React.FC<{{pascalCase name}}Props> = ({
  children,
  className,
}) => {
  return (
    <div{{#if withStyles}} className={`${styles.{{camelCase name}} ${className || ''}`.trim()}{{else}}{{#if className}} className={className}{{/if}}{{/if}}>
      {children}
    </div>
  );
};

export default {{pascalCase name}};
