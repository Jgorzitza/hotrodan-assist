import { describe, test } from 'vitest';

describe.skip('orders loader integration', () => {
  test('hydrates orders list from seed data', async () => {
    // TODO: replace with Remix loader harness once data layer is wired.
  });
});
